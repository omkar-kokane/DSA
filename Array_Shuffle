class Solution(object):
    def shuffle(self, nums, n):
        ans = []  
        for i in range(n):
            ans.append(nums[i])   # Pick element from first half
            ans.append(nums[n + i])  # Pick element from second half
        return ans
